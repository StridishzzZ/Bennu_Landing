# 实验结果汇总

运行方式：`python experiments/expN_*.py`，结果写入 `outputs/`。

## 实验 7 · 参数冗余审计与精简（先看这里）

原始 21 个科学参数中有大量常数或完全重复参数（贝努 7/21 零方差、龙宫 18/21 零方差），
精简为 **10 个科学参数**（完整参数归档在 `data/full/`）：

- C（3）：organic_abundance、ch2_ch3、carbon_content_pct
  （volatile_abundance、organic_silicate_ratio 合并入 organic_abundance）；
- M（2）：mineral_classes_detected（六类矿物检出合并）、hydration_depth
  （六类矿物 flag 与 amorphous_fraction 删除）；
- G（4）：space_weathering_proxy、crater_freshness、activity_level、psfd_class
  （brittle_deformation、geologic_diversity 删除）；
- T（1）：temp_rel_global（tmax_k 降为纯工程安全门控输入）。

精简前后验证（exp7_redundancy_audit.py）：

| 数据集 | 科学分排序 | 综合分排序（验收标准） |
|---|---|---|
| 贝努四区 | 一致 ✓（Nightingale 第一） | 一致 ✓ |
| 龙宫七区 | L07/L08 近并列交换 ✗（Δ≈0.0000） | 一致 ✓（L08/L07/M04 前三） |
| 龙宫子区 | 一致 ✓ | 一致 ✓（L08-E1 第一） |

说明：L07/L08 纯科学分并列（均为 0.577），二者区分完全由工程层（安全/可采样性）完成，
这正是文献"科学+安全+可采样求和"决策方式的体现。

## 实验 1 · 贝努四候选区复现验证

文献优先级权重下 ISVM 排序：

| 排名 | 站点 | ISVM | 说明 |
|---|---|---|---|
| 1 | Nightingale | 0.770 | 年轻陨石坑、北部低温、细粒丰富，与任务实际选择一致 |
| 2 | Sandpiper | 0.605 | 中纬、相对新鲜 |
| 3 | Kingfisher | 0.592 | 赤道陨石坑内 |
| 4 | Osprey | 0.577 | 科学分最低，但可达性 0.60 为四区最高（备选点定位） |

结论：Nightingale 科学价值第一；Osprey 的价值体现在工程层而非科学层，
与 Enos et al. 2020 的任务叙事一致。

## 实验 2 · 龙宫七候选区多级下选复现

纯科学 ISVM：L07 与 L08 并列第一（0.577），L05/L12 次之（0.574），M 区垫底
（空间风化较重，符合 L 区偏蓝/M 区偏红的文献判断）。

综合分（科学 + 安全 + 可采样 + 可达，参照 Yabuta et al. 2019 的分数求和做法）：

| 排名 | 站点 | 综合分 |
|---|---|---|
| 1 | L08 | 2.001 |
| 2 | L07 | 1.994 |
| 3 | M04 | 1.926 |

**前三名恰好为文献最终 TD1 候选 L08/L07/M04。** M04 科学分垫底但靠低块石覆盖率
（安全）与较高可采样性进入前三，复现"工程约束修正科学排序"的核心逻辑。

子区下选：综合分 L08-E1（2.415）> L08-B（1.839），L08-E1 为实际首次 touchdown 点。

## 实验 3 · 权重方案对比

| 方案 | C | M | G | T | Nightingale 排名 |
|---|---|---|---|---|---|
| 优先级（文献） | 0.35 | 0.30 | 0.20 | 0.15 | 1 |
| 等权 | 0.25 | 0.25 | 0.25 | 0.25 | 1 |
| 熵权 | 0.016 | 0.019 | 0.284 | 0.681 | 1 |

三种方案排序完全一致（Spearman = 1.0）。熵权在小样本同质数据下把权重压向 G/T，
是方法的预期行为而非指标不重要。

## 实验 4 · 敏感性分析

- 权重 ±20%：所有扰动下排序不变；
- 科学参数 ±10%：Nightingale 仍第一，无换位；
- 对 ISVM 数值影响最大的参数：hydration_depth（±0.012）、mineral_classes_detected（±0.011）、
  organic_abundance（±0.008）。

## 实验 5 · 蒙特卡洛不确定性

2000 次抽样（科学参数 ±10%）下，精简后排序完全稳定：

| 排名 | Nightingale | Sandpiper | Kingfisher | Osprey |
|---|---|---|---|---|
| 1 | 100% | 0% | 0% | 0% |
| 2 | 0% | 100% | 0% | 0% |
| 3 | 0% | 0% | 100% | 0% |
| 4 | 0% | 0% | 0% | 100% |

此前第 2–4 名的概率交织来自冗余参数的关联扰动，参数精简后自然消除。

## 实验 6 · 合成数据压力测试

- 30 个随机候选区：得分全部在 [0,1]、无 NaN；
- 全零站点 ISVM≈0.04，全满站点≈0.90；
- 缺失参数自动按中性值处理，流水线不崩溃。

## 实验 9 · 科学价值全球栅格化（Bennu 全幅 GeoTIFF）

把评分结果铺到 Bennu 全幅地图，并输出矿物丰度图。产物见 `outputs/rasters/`，
自检清单 `outputs/exp9_raster_manifest.csv`。

**栅格规格**：等经纬度 720×360（0.5°/像元，≈2.14 m/像元），纬向 90°N→90°S，
经度 0–360°E，带地理变换与投影，波段描述含中文语义。

**四通道地图** `bennu_isvm_4band.tiff`（= ISVM 四部分 / 文献四张科学价值图）：

| band | 通道 | 含义 | min | max | mean |
|---|---|---|---|---|---|
| 1 | `isvm_C` | SVCCM 化学组成 | 0.6133 | 0.6933 | 0.6139 |
| 2 | `isvm_M` | SVMM 矿物学 | 0.6833 | 0.7749 | 0.6839 |
| 3 | `isvm_G` | SVGFM 地质特征/新鲜度 | 0.4125 | 0.7248 | 0.4888 |
| 4 | `isvm_T` | SVTM 温度/热环境 | 0.5000 | 0.9996 | 0.5030 |

综合图 `bennu_isvm_total.tiff`（priority 权重）：min 0.5772 / max 0.7700 / mean 0.5932。

**空间化规则**：候选区足迹内取该区评分，全球其余部分取 `data/global_baseline.csv`
得到的背景指示分（C=0.613、M=0.683、G=0.488、T=0.500），两者按高斯足迹核平滑过渡。
站点坐标取自 Burke et al. 2021 的 ROI 中心表，足迹半径统一 25 m（设计假设）。

**验收自检（全部通过）**：

- 四个站点中心像元值与 `exp1` 评分逐通道一致（误差 < 0.02）；
- 远离候选区的三个背景点均等于基线值；
- 6 波段丰度图逐像元总和恒为 1.000000；
- 4 通道地图波段名/标签/地理变换/投影回读正确，无 nodata 像元。

**矿物丰度图的来源与限制**：仓库内没有 Bennu 矿物丰度栅格（见 `MINERAL_ABUNDANCE.md`），
因此默认输出 Tier-1 **代理丰度场**（文献锚点 + 固定种子 20260912，可复现）。
代理场给出 6 类矿物均值 phyllo 0.599 / silicate 0.168 / oxide 0.118 / amorphous 0.068 /
carbonate 0.025 / sulfate 0.023，折算「关键矿物类别数」全球均值 4.01。
拿到 PDS 的 OVIRS/OTES 产品后，用 `--mineral-raster` 传入即可替换为真实反演结果。

## 实验 10 · 叠加到真实 Bennu DOM/DEM 底图

数据：用户提供的 5 个全球 GeoTIFF（31417×15709，≈5 cm/px，合计 6.6 GB），
检查与坑位见 `data/planetary/README.md`。实验 10 使用 DEG 版本（地理经纬度），
10× 降采样到 3141×1570（≈0.49 m/px）。

**套合验证（全部通过）**：

- 输出的四通道图与 DOM 底图 shape 与 geotransform **逐位一致**，GIS 中可直接叠加；
- 四个站点像元值与 `exp1` 评分逐通道一致；
- 底图统计 mean 75.6 / std 60.6，ASCII 缩略图呈赤道亮带与两极暗区的真实结构，
  非空值或纯色。

**真实坡度 vs 项目假设值（ROI 尺度，DEM 实测）**：

| 站点 | ROI 半径 | 实测中位坡度 | p90 | 原假设 | 门限 14° |
|---|---|---|---|---|---|
| Nightingale | 2.12 m | 12.8° | 15.5° | 5.0° | 中位数通过、p90 不通过 |
| Osprey | 1.51 m | 17.3° | 22.5° | 6.0° | **不通过** |
| Kingfisher | 3.88 m | 13.2° | 20.9° | 6.0° | 中位数通过、p90 不通过 |
| Sandpiper | 4.38 m | 9.4° | 12.4° | 6.0° | 通过 |

交叉验证：Barnouin et al. 2022（`articles/1-…pdf`）给出 Nightingale 接触点局部坡度
**8°–10°**，与本文 12.8°（2.12 m 半径圆域中位）量级一致；坡度在 0.05–2 m 多个平滑
尺度上稳定（12.84°→12.80°），排除 5 cm 噪声或垂直夸张的可能。
因此 `bennu_sites.csv` 中的 5°–6° 假设值偏低 1.6–2.9 倍，需要更新。

**产出**：`outputs/rasters/bennu_dom_base.tiff`（6.3 MB）、
`bennu_isvm_4band_domgrid.tiff`（2.5 MB）、`preview_real_basemap.png`、
`outputs/exp10_site_terrain.csv`。

**遗留问题**：① DEM 垂直参考面待向数据提供方确认（全球高程跨度 76 m，
大于形状模型 18 m 的半径变化）；② 安全门控判据需确定用中位数、p90 还是面积占比；
③ 四通道目前高度相关（r≈0.97），若需真正独立的空间结构，需做 Tier-1 相分类。

## 实验 11 · 着陆点科学价值与参数贡献度地图

在真实 Bennu DOM 影像上表达着陆点科学价值，并把 ISVM 完整分解到 10 个科学参数。
按用户设定：**不用坡度等工程门控**、只用现有程序的科学参数、只表达四个候选区
（区外 nodata，不做背景填充）。

**贡献度定义**：`c(p) = s(p) × w(指标) / n(指标内参数数)`，即把指标权重均分到
该指标内的参数上。10 个 c(p) 逐像元求和恒等于 ISVM——栅格实测最大偏差 6e-8
（float32 舍入），CSV 汇总偏差 <1e-12，分解严格闭合。

各参数有效权重：C 层三参数各 0.1167；M 层两参数各 0.150；G 层四参数各 0.050；
T 层单参数 0.150。

**四站点贡献度占比（%，每列合计 100%）**：

| 参数 | 有效权重 | Nightingale | Osprey | Kingfisher | Sandpiper |
|---|---|---|---|---|---|
| temp_rel_global 温度 | 0.150 | **19.5** | 13.0 | 12.7 | 12.4 |
| hydration_depth 水合带深 | 0.150 | 15.6 | **18.2** | **17.7** | **17.3** |
| mineral_classes_detected 矿物类别数 | 0.150 | 14.6 | 17.3 | 16.9 | 16.5 |
| ch2_ch3 | 0.1167 | 13.3 | 17.0 | 16.5 | 16.6 |
| organic_abundance 有机物 | 0.1167 | 10.6 | 12.1 | 11.8 | 11.6 |
| carbon_content_pct 碳含量 | 0.1167 | 7.6 | 8.1 | 7.9 | 8.7 |
| crater_freshness 坑新鲜度 | 0.050 | 6.5 | 5.2 | 5.1 | 5.0 |
| psfd_class | 0.050 | 6.5 | 3.0 | 5.5 | 5.4 |
| space_weathering_proxy | 0.050 | 4.5 | 4.3 | 4.2 | 5.0 |
| activity_level 活动证据 | 0.050 | 1.3 | 1.7 | 1.7 | 1.7 |

**结论**：

- Nightingale 领先的主因是 **temp_rel_global（19.5%）与 crater_freshness（6.5%）**，
  这两项正好对应文献中"年轻 Hokioi 坑 + 北部低温利于挥发分保存"的选址理由；
- 其余三个站点的最大贡献参数都是 **hydration_depth（17–18%）**，
  因为它们在该参数上取值相同（0.7），排序差异主要来自 G 层
  （Osprey 的 psfd_class=coarse 仅贡献 3.0%，拖低其 ISVM 至 0.577）；
- 四个站点的 activity_level 贡献恒为 1.3–1.7%、ch2_ch3 相差仅 0.5%，
  属**无区分度参数**，在贡献图上表现为四站同色；
- 贡献度差异最大的参数是 temp_rel_global（12.4–19.5%，跨度 7.1%）与
  mineral_classes_detected（14.6–17.3%）。

**产出**：`bennu_isvm_landing_value.tiff`、`bennu_param_contribution.tiff`（10 波段）、
`bennu_param_contribution_share.tiff`（10 波段占比）、`fig_landing_value_on_bennu.png`、
`fig_dom_global_sites.png`（真实 DOM 全域大图 + 四站点标注，3600×1875）、
`fig_param_<参数>.png` ×10、`fig_contribution_bars.png`、
`outputs/exp11_param_contribution.csv`。栅格与真实底图格网逐位一致。

## 实验 11 更新 · 全幅像元级填充（实装测试，2026-09-20）

原版实验 11 只表达四个候选区、区外 nodata。按用户要求改为**全幅像元级填充**，
用于验证流程完整性、性能与成品外观。

**填充规则**：候选区沿用站点级评分（与实验 1 完全一致）；区外为合成占位数据——
连续参数取值限制在参数域"最差端"5% 区间内并叠加固定种子平滑噪声（评分 ∈ [0, 0.05]），
分类参数取评分最低档位。种子 20260920，可复现。

**验收结果（全部通过）**：

| 检查项 | 结果 |
|---|---|
| 覆盖度 | 4.93 M 像元全部有值，无 nodata（候选区 43,333 像元 = 0.88%） |
| 候选区数值是否被影响 | 四站点 10 个参数贡献度与改动前**逐项相等**；ISVM 0.7702/0.5772/0.5922/0.6053 完全一致 |
| 分解闭合性 | 逐像元 Σ10 贡献度 = ISVM，最大偏差 **0.000e+00**；Σ占比 = 100%，偏差 2.3e-5 |
| 与候选区的差距 | 填充区 ISVM ∈ [0.0494, 0.0710]，候选区 ∈ [0.5772, 0.7702]，**填充最大值 < 候选最小值，相差 0.506** |
| 逐参数差距 | 10 个参数中 8 个填充贡献严格小于候选区最小值；`activity_level`、`psfd_class` 因候选区本就取最低档而相等（分类评分离散的固有结果） |
| 成品外观 | 全域面板近白像素仅 0.44%（无空洞），亮度标准差 23.0（真实底图纹理仍可见），候选区高亮 |

**性能**：全流程 36.4 s（填充+评分 1.1 s、栅格写出 9.7 s、出图约 25 s）。
10 个参数的填充场生成 + 评分在 1.1 s 内完成，说明当前 numpy 向量化路径在全幅规模下无性能瓶颈。

**工程后果（值得注意）**：全幅 float32 栅格体积大幅增长——贡献度 10 波段 73 MB、
占比 10 波段 116 MB，合计 207 MB；占比栅格已超过 GitHub 单文件 100 MB 上限。
因此这两个大栅格不再入库（本地保留、可 36 s 重跑），入库的是成品图、ISVM 单波段、
掩膜 `bennu_fill_mask.tiff` 与 CSV。若后续要做正式全幅产品，建议改用 uint16 + 比例因子
（体积约减半）或对填充场使用更粗的相关尺度（提升压缩率）。

## 总体结论与注意事项

1. 指标体系与文献对齐（四张科学价值图），在"直接给定参数"模式下复现贝努/龙宫真实选址；
2. 工程层是必要补充——龙宫 M04 入选、L08/L07 区分均依赖工程层；
3. 参数精简（21 → 10）不改变任何验收结论，且消除了冗余参数带来的假性排序不确定性；
4. 当前低置信度参数：Kingfisher/Sandpiper 及龙宫工程参数仍为假设值，正式使用前需替换；
5. 完整参数定义保留在 `data/full/` 与 `src/config.py`，未来接入真实数据时可重新启用；
6. 实验 9 已把科学价值栅格化并输出四通道 GeoTIFF，但 M 层的矿物丰度目前是代理场，
   其空间区分度、而非全球均值，才是该图的科学增量（`MINERAL_ABUNDANCE.md` 第 4.3 节）；
7. 实验 10 接入真实 DOM/DEM 后，**四站点实测坡度（9.4°–17.3°）系统性高于原假设值
   （5°–6°）**，Osprey 在 14° 安全门限下不再通过——这是接入真实地形后暴露出的
   最实质问题，需在确定门控判据后重跑实验 1/4/5；
8. 实验 11 给出了着陆点科学价值的真实地图表达与 10 个参数的贡献度分解，
   分解严格闭合（Σc = ISVM）；Nightingale 的优势来自温度与坑新鲜度，
   其余三站点的最大贡献参数均为水合带深，且 activity_level、ch2_ch3
   在四站点间几乎无区分度。
